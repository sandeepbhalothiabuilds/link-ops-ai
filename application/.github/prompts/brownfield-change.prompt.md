# Brownfield change prompt

Inspect the repository map before suggesting a change. Identify impacted modules,
routes, data flows, tests, blast radius, and the smallest compatible implementation.
Mark downstream tasks stale only when their recorded input hashes are invalidated.
