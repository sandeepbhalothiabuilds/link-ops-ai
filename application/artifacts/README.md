# Generated workflow artifacts

Workflow runs write generated evidence under `artifacts/<run_id>/`. Generated run
directories are ignored by Git; retain or export a selected evidence bundle explicitly
when a demo requires it.
