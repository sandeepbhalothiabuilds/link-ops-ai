# Security guardrails

Accept only HTTP and HTTPS destination URLs. Reject embedded credentials and unsafe
aliases. Use conditional writes for uniqueness, least-privilege IAM, path sandboxing,
tool allowlists, and human approval before push or cloud deployment. Never retain raw
IP addresses or complete user-agent strings for aggregate analytics.
