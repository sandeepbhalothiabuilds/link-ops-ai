# Git change management

Create a savepoint before high-risk writes. Keep commits small and aligned to coherent
workstreams. Run tests, lint, type checks, and security checks before a release or push.
External side effects require an immutable approval record.
