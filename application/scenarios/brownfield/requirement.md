# Brownfield scenario

Add link expiration to the existing service without breaking current clients. The run
must analyze the repository first, identify the blast radius, preserve existing routes,
and generate a regression-test expectation.
